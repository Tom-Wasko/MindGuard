package com.mindguard.core.ui.theme

import androidx.compose.ui.graphics.Color

// === MIDNIGHT STUDY PALETTE (Dark Mode - Primary) ===

// Backgrounds
val MidnightInk = Color(0xFF181824)       // Main background
val MidnightSurface = Color(0xFF242234)    // Cards, containers
val MidnightSurfaceVar = Color(0xFF2D2942) // Elevated cards
val MidnightOverlay = Color(0xFF1E1D30)   // Dialog/sheet bg

// Accent colors
val WarmAmber = Color(0xFFE6C594)          // Primary: warm lamp glow
val SageGreen = Color(0xFFA3B899)          // Secondary: calming sage
val DustyTerracotta = Color(0xFFE0A899)    // Tertiary: warm emotional
val SoftLavender = Color(0xFF9B8EC4)       // For mindfulness UI

// Text
val CreamWhite = Color(0xFFEAE6E1)         // Primary text
val MutedLilac = Color(0xFF9E9AA7)         // Secondary text
val DimText = Color(0xFF6B677A)            // Disabled/hint text

// Semantic
val PanicRed = Color(0xFFE94560)           // Panic protocol, errors
val SuccessGreen = Color(0xFF7DB87A)       // Correct answers, completion
val WarmGlow = Color(0x26E6C594)           // Amber glow (15% alpha)
val PanicGlow = Color(0x40E94560)          // Red glow for panic

// === VINTAGE TEA & PAPER PALETTE (Light Mode) ===
val PaperCream = Color(0xFFF5EFEB)         // Light background
val PaperSurface = Color(0xFFEBE3DB)       // Light card surface
val EucalyptusGreen = Color(0xFF8FA69B)    // Light primary
val CaramelMilk = Color(0xFFC79B7B)        // Light accent
val InkSlate = Color(0xFF2B2D42)           // Light text
val WarmGray = Color(0xFF8D8D99)           // Light secondary text

// Breathing exercise phase colors
val InhaleBlue = Color(0xFF7EC8E3)         // Inhale phase
val HoldYellow = Color(0xFFE6C594)         // Hold phase
val ExhaleGreen = Color(0xFFA3B899)        // Exhale phase
